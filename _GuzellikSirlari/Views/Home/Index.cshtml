@{
    ViewBag.Title = "Home Page";
}

@{ Html.RenderAction("Menu1", "PartialManager"); }
   @{ Html.RenderAction("Menu2", "PartialManager"); }
@{ Html.RenderAction("Menu3", "PartialManager"); }
@{ Html.RenderAction("Menu4", "PartialManager"); }
   @{ Html.RenderAction("Menu5", "PartialManager"); }
   
  